import { BaseComponent } from '../components/base.component';

export declare type onCompleteCallback = (self: BaseComponent) => void;
export declare type onErrorCallback = (self: BaseComponent, error: any) => void;
export declare type onSuccessCallback = (self: BaseComponent, data: any) => void;
